[pytest]
addopts = -v --tb=short
markers =
    selenium: browser tests driven with Selenium
    e2e: full end-to-end tests that build and run a real pipeline on the cluster
filterwarnings =
    ignore::DeprecationWarning
