// Package registry provides shared types for service configuration.
// Self-registration has been removed; services are registered manually
// via the registry admin API (POST /services).
package registry
