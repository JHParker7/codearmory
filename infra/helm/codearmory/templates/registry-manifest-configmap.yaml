apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "codearmory.fullname" . }}-registry-manifest
  labels:
    {{- include "codearmory.labels" . | nindent 4 }}
    app.kubernetes.io/component: registry
data:
  manifest.json: |
{{ .Files.Get "files/registry-manifest.json" | indent 4 }}
