{{- if and .Values.argo.enabled .Values.outpostGateway.enabled .Values.tests.argo.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "codearmory.fullname" . }}-test-argo
  labels:
    {{- include "codearmory.labels" . | nindent 4 }}
    app.kubernetes.io/component: test-argo
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation
    helm.sh/hook-weight: "20"
spec:
  restartPolicy: Never
  {{- with .Values.imagePullSecrets }}
  imagePullSecrets:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  containers:
    - name: test
      image: "{{ include "codearmory.image" (list . .Values.tests.argo) }}"
      imagePullPolicy: {{ .Values.imagePullPolicy }}
      env:
        - name: ARGO_URL
          value: "http://{{ include "codearmory.fullname" . }}-argo:{{ .Values.argo.port }}"
        - name: OUTPOST_GATEWAY_URL
          value: "http://{{ include "codearmory.fullname" . }}-outpost-gateway:{{ .Values.outpostGateway.port }}"
        - name: GATEKEEPER_URL
          value: "http://{{ include "codearmory.fullname" . }}-gatekeeper:{{ .Values.gatekeeper.port }}"
{{- end }}
