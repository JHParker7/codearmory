{{- if .Values.events.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "codearmory.fullname" . }}-events
  labels:
    {{- include "codearmory.labels" . | nindent 4 }}
    app.kubernetes.io/component: events
spec:
  # NodePort when a git host must reach the webhook endpoint from outside the cluster
  # (webevents bypass conductor — they authenticate by HMAC, not a codearmory bearer).
  # ClusterIP otherwise; front it with an Ingress in a real deployment.
  type: {{ .Values.events.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.events.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq (.Values.events.service.type | default "ClusterIP") "NodePort") .Values.events.service.nodePort }}
      nodePort: {{ .Values.events.service.nodePort }}
      {{- end }}
  selector:
    app.kubernetes.io/name: {{ include "codearmory.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: events
{{- end }}
