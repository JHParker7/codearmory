{{- if not .Values.global.externalSecrets }}
{{- if .Values.notifications.enabled }}
{{- $fullName := include "codearmory.fullname" . -}}
{{- $existing := lookup "v1" "Secret" .Release.Namespace (printf "%s-notifications" $fullName) -}}
{{- $gkKey := .Values.notifications.secret.gatekeeperServiceKey -}}
{{- if not $gkKey -}}
  {{- if and $existing (index $existing.data "gatekeeper-service-key") -}}
    {{- $gkKey = index $existing.data "gatekeeper-service-key" | b64dec -}}
  {{- else -}}
    {{- $gkKey = include "codearmory.bootstrapServiceKey" (list $ "notifications") -}}
  {{- end -}}
{{- end -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $fullName }}-notifications
  labels:
    {{- include "codearmory.labels" . | nindent 4 }}
    app.kubernetes.io/component: notifications
type: Opaque
stringData:
{{- if .Values.notifications.secret.databaseUrl }}
  database-url: {{ .Values.notifications.secret.databaseUrl | quote }}
{{- end }}
  gatekeeper-service-key: {{ $gkKey | quote }}
{{- end }}
{{- end }}
