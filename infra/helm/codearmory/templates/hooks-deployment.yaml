{{- if .Values.hooks.enabled }}
{{- $fullname := include "codearmory.fullname" . }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $fullname }}-hooks
  labels:
    {{- include "codearmory.labels" . | nindent 4 }}
    app.kubernetes.io/component: hooks
spec:
  replicas: {{ .Values.hooks.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "codearmory.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: hooks
  template:
    metadata:
      labels:
        {{- include "codearmory.labels" . | nindent 8 }}
        app.kubernetes.io/component: hooks
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 100
        runAsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        {{- include "codearmory.initContainer.waitForGatekeeper" . | nindent 8 }}
      containers:
        - name: hooks
          image: "{{ include "codearmory.image" (list . .Values.hooks) }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: {{ .Values.hooks.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.hooks.port | quote }}
            - name: LOG_LEVEL
              value: {{ .Values.hooks.env.logLevel | quote }}
            - name: GATEKEEPER_URL
              value: "http://{{ $fullname }}-gatekeeper:{{ .Values.gatekeeper.port }}"
            - name: WORKFLOWS_URL
              value: "http://{{ $fullname }}-workflows:{{ .Values.workflows.port }}"
            {{- include "codearmory.otel.envs" (list . .Values.hooks.env) | nindent 12 }}
            {{- include "codearmory.postgresql.env" (list . "hooks" "hooks" $fullname) | nindent 12 }}
            - name: GATEKEEPER_SERVICE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $fullname }}-hooks
                  key: gatekeeper-service-key
            - name: HOOKS_TRIGGER_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $fullname }}-hooks
                  key: hooks-trigger-key
            {{- include "codearmory.tls.envs" .Values.hooks.tls | nindent 12 }}
          {{- with (include "codearmory.tls.volumeMounts" .Values.hooks.tls) }}
          volumeMounts:
            {{- . | nindent 12 }}
          {{- end }}
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.hooks.resources | nindent 12 }}
      {{- with (include "codearmory.tls.volumes" .Values.hooks.tls) }}
      volumes:
        {{- . | nindent 8 }}
      {{- end }}
{{- end }}
