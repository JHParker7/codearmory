{{- if .Values.events.enabled }}
{{- $fullname := include "codearmory.fullname" . }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $fullname }}-events
  labels:
    {{- include "codearmory.labels" . | nindent 4 }}
    app.kubernetes.io/component: events
spec:
  replicas: {{ .Values.events.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "codearmory.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: events
  template:
    metadata:
      labels:
        {{- include "codearmory.labels" . | nindent 8 }}
        app.kubernetes.io/component: events
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 100
        runAsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        {{- include "codearmory.initContainer.waitForGatekeeper" . | nindent 8 }}
      containers:
        - name: events
          image: "{{ include "codearmory.image" (list . .Values.events) }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: {{ .Values.events.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.events.port | quote }}
            - name: LOG_LEVEL
              value: {{ .Values.events.env.logLevel | quote }}
            - name: GATEKEEPER_URL
              value: "http://{{ $fullname }}-gatekeeper:{{ .Values.gatekeeper.port }}"
            # Where events dispatches a matched rule's run. It signs the call with
            # events-trigger-key, which the workflows service verifies.
            - name: WORKFLOWS_URL
              value: "http://{{ $fullname }}-workflows:{{ .Values.workflows.port }}"
            {{- if .Values.tickets.enabled }}
            # Target of the create_ticket action.
            - name: TICKETS_URL
              value: "http://{{ $fullname }}-tickets:{{ .Values.tickets.port }}"
            {{- end }}
            {{- include "codearmory.otel.envs" (list . .Values.events.env) | nindent 12 }}
            {{- include "codearmory.postgresql.env" (list . "events" "events" $fullname) | nindent 12 }}
            - name: GATEKEEPER_SERVICE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $fullname }}-events
                  key: gatekeeper-service-key
            - name: EVENTS_TRIGGER_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $fullname }}-events
                  key: events-trigger-key
            # Secret the git provider's webhook is configured with; events verifies the
            # provider HMAC on the public POST /hooks/git against it. Optional because an
            # unset value keeps that endpoint closed rather than open.
            - name: EVENTS_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ $fullname }}-events
                  key: events-webhook-secret
                  optional: true
            {{- if .Values.events.githubApp.appId }}
            # GitHub App: enables POST /hooks/github, which reports each triggered pipeline
            # run back to GitHub as a check run on the pushed commit. All three values are
            # required together — events refuses to start with an app id but no webhook
            # secret, rather than accepting unverified deliveries.
            - name: GITHUB_APP_ID
              value: {{ .Values.events.githubApp.appId | quote }}
            {{- with .Values.events.githubApp.apiUrl }}
            - name: GITHUB_API_URL
              value: {{ . | quote }}
            {{- end }}
            - name: GITHUB_APP_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $fullname }}-events
                  key: github-app-private-key
            - name: GITHUB_APP_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ $fullname }}-events
                  key: github-app-webhook-secret
            {{- end }}
            {{- include "codearmory.tls.envs" .Values.events.tls | nindent 12 }}
          {{- with (include "codearmory.tls.volumeMounts" .Values.events.tls) }}
          volumeMounts:
            {{- . | nindent 12 }}
          {{- end }}
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.events.resources | nindent 12 }}
      {{- with (include "codearmory.tls.volumes" .Values.events.tls) }}
      volumes:
        {{- . | nindent 8 }}
      {{- end }}
{{- end }}
