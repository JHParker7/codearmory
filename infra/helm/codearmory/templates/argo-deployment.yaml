{{- if .Values.argo.enabled }}
{{- $full := include "codearmory.fullname" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $full }}-argo
  labels:
    {{- include "codearmory.labels" . | nindent 4 }}
    app.kubernetes.io/component: argo
spec:
  replicas: {{ .Values.argo.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "codearmory.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: argo
  template:
    metadata:
      labels:
        {{- include "codearmory.labels" . | nindent 8 }}
        app.kubernetes.io/component: argo
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 100
        runAsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        {{- include "codearmory.initContainer.waitForGatekeeper" . | nindent 8 }}
      containers:
        - name: argo
          image: "{{ include "codearmory.image" (list . .Values.argo) }}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: {{ .Values.argo.port }}
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.argo.port | quote }}
            - name: LOG_LEVEL
              value: {{ .Values.argo.env.logLevel | quote }}
            - name: GATEKEEPER_URL
              value: "http://{{ $full }}-gatekeeper:{{ .Values.gatekeeper.port }}"
            {{- if .Values.outpostGateway.enabled }}
            - name: OUTPOST_GATEWAY_URL
              value: "http://{{ $full }}-outpost-gateway:{{ .Values.outpostGateway.port }}"
            {{- end }}
            {{- include "codearmory.otel.envs" (list . .Values.argo.env) | nindent 12 }}
            {{- include "codearmory.postgresql.env" (list . "argo" "argo" $full) | nindent 12 }}
            - name: GATEKEEPER_SERVICE_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $full }}-argo
                  key: gatekeeper-service-key
            - name: OUTPOST_INTERNAL_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ $full }}-argo
                  key: outpost-internal-key
            {{- include "codearmory.tls.envs" .Values.argo.tls | nindent 12 }}
          {{- with (include "codearmory.tls.volumeMounts" .Values.argo.tls) }}
          volumeMounts:
            {{- . | nindent 12 }}
          {{- end }}
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.argo.resources | nindent 12 }}
      {{- with (include "codearmory.tls.volumes" .Values.argo.tls) }}
      volumes:
        {{- . | nindent 8 }}
      {{- end }}
{{- end }}
