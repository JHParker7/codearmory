{{- if not .Values.global.externalSecrets }}
{{- $fullName := include "codearmory.fullname" . -}}
{{- $redisUrl := coalesce .Values.gatekeeper.secret.redisUrl (ternary (printf "redis://%s-gatekeeper-redis:6379/0" $fullName) "" .Values.gatekeeper.redis.enabled) }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace (printf "%s-gatekeeper" $fullName) -}}
{{- $registryKey := .Values.gatekeeper.secret.registryServiceKey -}}
{{- if not $registryKey -}}
  {{- if and $existing (index $existing.data "registry-service-key") -}}
    {{- $registryKey = index $existing.data "registry-service-key" | b64dec -}}
  {{- else -}}
    {{- $registryKey = include "codearmory.bootstrapServiceKey" (list $ "gatekeeper") -}}
  {{- end -}}
{{- end -}}
{{- /* builder-internal-key: shared with builder so the org-service disable-gate
       can read effective state. Resolve from builder's secret (or the matching
       bootstrap seed) so both sides agree on first install. */ -}}
{{- $builderInternalKey := "" -}}
{{- if .Values.builder.enabled -}}
  {{- $builderInternalKey = .Values.builder.secret.internalKey -}}
  {{- if not $builderInternalKey -}}
    {{- $builderSecret := lookup "v1" "Secret" .Release.Namespace (printf "%s-builder" $fullName) -}}
    {{- if and $builderSecret (index $builderSecret.data "builder-internal-key") -}}
      {{- $builderInternalKey = index $builderSecret.data "builder-internal-key" | b64dec -}}
    {{- else -}}
      {{- $builderInternalKey = include "codearmory.bootstrapServiceKey" (list $ "builder-internal") -}}
    {{- end -}}
  {{- end -}}
{{- end -}}
{{- /* gatekeeper-secrets-key: the AES-256-GCM key (64 hex chars = 32 bytes) that
       encrypts stored user/org secrets. Without it the secrets feature is disabled
       (endpoints 503), which also blocks secret: job refs. Resolve from an explicit
       value, else the existing in-cluster key (so it survives upgrades — rotating it
       would orphan every stored ciphertext), else a deterministic bootstrap key.
       sha256sum emits exactly 64 hex chars, matching the required key format. */ -}}
{{- $secretsKey := .Values.gatekeeper.secret.secretsKey -}}
{{- if not $secretsKey -}}
  {{- if and $existing (index $existing.data "gatekeeper-secrets-key") -}}
    {{- $secretsKey = index $existing.data "gatekeeper-secrets-key" | b64dec -}}
  {{- else -}}
    {{- $secretsKey = include "codearmory.bootstrapServiceKey" (list $ "gatekeeper-secrets-enc") -}}
  {{- end -}}
{{- end -}}
{{- /* Assemble gatekeeper-services — the startup allowlist of CORE service identities
       (builder, registry, forge, workflows). Non-core services are deployed by builder and
       register their gatekeeper identity at runtime (builder POSTs /internal/service-accounts);
       gatekeeper then auto-permits them via the live ServiceAccount lookup, so they are NOT
       seeded here. On first install each key is the deterministic bootstrapServiceKey (the
       same helper each service uses), so both sides agree without a second upgrade. */ -}}
{{- $gkServices := .Values.gatekeeper.secret.gatekeeperServices -}}
{{- if not $gkServices -}}
  {{- $parts := list -}}
  {{- if .Values.builder.enabled -}}
    {{- $svc := lookup "v1" "Secret" .Release.Namespace (printf "%s-builder" $fullName) -}}
    {{- $k := "" -}}
    {{- if and $svc (index $svc.data "gatekeeper-service-key") -}}
      {{- $k = index $svc.data "gatekeeper-service-key" | b64dec -}}
    {{- else -}}
      {{- $k = include "codearmory.bootstrapServiceKey" (list $ "builder") -}}
    {{- end -}}
    {{- $parts = append $parts (printf "builder=%s" $k) -}}
  {{- end -}}
  {{- if .Values.artifacts.enabled -}}
    {{- $svc := lookup "v1" "Secret" .Release.Namespace (printf "%s-artifacts" $fullName) -}}
    {{- $k := "" -}}
    {{- if and $svc (index $svc.data "gatekeeper-service-key") -}}
      {{- $k = index $svc.data "gatekeeper-service-key" | b64dec -}}
    {{- else -}}
      {{- $k = include "codearmory.bootstrapServiceKey" (list $ "artifacts") -}}
    {{- end -}}
    {{- $parts = append $parts (printf "artifacts=%s" $k) -}}
  {{- end -}}
  {{- if .Values.forge.enabled -}}
    {{- $svc := lookup "v1" "Secret" .Release.Namespace (printf "%s-forge" $fullName) -}}
    {{- $k := "" -}}
    {{- if and $svc (index $svc.data "gatekeeper-service-key") -}}
      {{- $k = index $svc.data "gatekeeper-service-key" | b64dec -}}
    {{- else -}}
      {{- $k = include "codearmory.bootstrapServiceKey" (list $ "forge") -}}
    {{- end -}}
    {{- $parts = append $parts (printf "forge=%s" $k) -}}
  {{- end -}}
  {{- if .Values.workflows.enabled -}}
    {{- $svc := lookup "v1" "Secret" .Release.Namespace (printf "%s-workflows" $fullName) -}}
    {{- $k := "" -}}
    {{- if and $svc (index $svc.data "gatekeeper-service-key") -}}
      {{- $k = index $svc.data "gatekeeper-service-key" | b64dec -}}
    {{- else -}}
      {{- $k = include "codearmory.bootstrapServiceKey" (list $ "workflows") -}}
    {{- end -}}
    {{- $parts = append $parts (printf "workflows=%s" $k) -}}
  {{- end -}}
  {{- /* tickets, git and containers are core (Helm-deployed), so unlike
         builder-deployed services they are NOT registered at runtime — seed their keys here,
         like forge/workflows. */ -}}
  {{- if .Values.tickets.enabled -}}
    {{- $svc := lookup "v1" "Secret" .Release.Namespace (printf "%s-tickets" $fullName) -}}
    {{- $k := "" -}}
    {{- if and $svc (index $svc.data "gatekeeper-service-key") -}}
      {{- $k = index $svc.data "gatekeeper-service-key" | b64dec -}}
    {{- else -}}
      {{- $k = include "codearmory.bootstrapServiceKey" (list $ "tickets") -}}
    {{- end -}}
    {{- $parts = append $parts (printf "tickets=%s" $k) -}}
  {{- end -}}
  {{- if .Values.git.enabled -}}
    {{- $svc := lookup "v1" "Secret" .Release.Namespace (printf "%s-git" $fullName) -}}
    {{- $k := "" -}}
    {{- if and $svc (index $svc.data "gatekeeper-service-key") -}}
      {{- $k = index $svc.data "gatekeeper-service-key" | b64dec -}}
    {{- else -}}
      {{- $k = include "codearmory.bootstrapServiceKey" (list $ "git") -}}
    {{- end -}}
    {{- $parts = append $parts (printf "git_connector=%s" $k) -}}
  {{- end -}}
  {{- if .Values.containers.enabled -}}
    {{- $svc := lookup "v1" "Secret" .Release.Namespace (printf "%s-containers" $fullName) -}}
    {{- $k := "" -}}
    {{- if and $svc (index $svc.data "gatekeeper-service-key") -}}
      {{- $k = index $svc.data "gatekeeper-service-key" | b64dec -}}
    {{- else -}}
      {{- $k = include "codearmory.bootstrapServiceKey" (list $ "containers") -}}
    {{- end -}}
    {{- $parts = append $parts (printf "containers=%s" $k) -}}
  {{- end -}}
  {{- $svc := lookup "v1" "Secret" .Release.Namespace (printf "%s-registry" $fullName) -}}
  {{- $k := "" -}}
  {{- if and $svc (index $svc.data "gatekeeper-service-key") -}}
    {{- $k = index $svc.data "gatekeeper-service-key" | b64dec -}}
  {{- else -}}
    {{- $k = include "codearmory.bootstrapServiceKey" (list $ "registry") -}}
  {{- end -}}
  {{- $parts = append $parts (printf "registry=%s" $k) -}}
  {{- $gkServices = join "," $parts -}}
{{- end -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $fullName }}-gatekeeper
  labels:
    {{- include "codearmory.labels" . | nindent 4 }}
    app.kubernetes.io/component: gatekeeper
type: Opaque
stringData:
{{- if .Values.gatekeeper.secret.databaseUrl }}
  database-url: {{ .Values.gatekeeper.secret.databaseUrl | quote }}
{{- end }}
  redis-url: {{ $redisUrl | quote }}
  gatekeeper-services: {{ $gkServices | quote }}
  registry-service-key: {{ $registryKey | quote }}
{{- if .Values.builder.enabled }}
  builder-internal-key: {{ $builderInternalKey | quote }}
{{- end }}
  secrets-lookup-allowed-callers: {{ .Values.gatekeeper.env.secretsLookupAllowedCallers | default "forge,containers" | quote }}
  gatekeeper-secrets-key: {{ $secretsKey | quote }}
{{- if .Values.gatekeeper.secret.oidcSigningKey }}
  oidc-signing-key: {{ .Values.gatekeeper.secret.oidcSigningKey | quote }}
{{- end }}
{{- if .Values.gatekeeper.secret.adminPassword }}
  admin-password: {{ .Values.gatekeeper.secret.adminPassword | quote }}
{{- end }}
{{- end }}
